# 豆脚 Altera Excel 生成工具 - 功能说明

## 概述

豆脚 Altera Excel 生成工具是一款专为 Shopify 店铺批量导入数据设计的自动化工具。只需输入品牌、域名和产品 CSV 文件，即可一键生成包含 Metafield 定义、智能集合、条款页面、导航菜单等完整数据的 Excel 文件，并自动定制 Shopify 主题模板。

---

## 核心功能

### 1. Excel 数据生成

根据产品 CSV 文件自动生成 Altera 导入所需的 Excel 文件，包含以下工作表：

| 工作表 | 说明 |
|--------|------|
| **Metafield Definitions** | 从 CSV 的 `c_f` / `product.metafields` 列提取，自动生成自定义字段定义 |
| **Smart Collections** | 从 CSV 的 `Tags` 列提取所有唯一标签，每个标签生成一个智能集合 |
| **Pages** | 自动生成 8 个条款页面，每个页面从 8 个版本中随机挑选一个 |
| **Menu** | 智能生成商品分类菜单（来自 Smart Collections）+ 条款分组菜单（来自 Pages） |

> **大数据量处理**：当 Smart Collections 或 Menu 超过 99 行时，自动从 Excel 中移除，输出到独立的 CSV 文件（存放在 `altera/` 目录），避免 Altera 导入限制。

---

### 2. 条款页面生成（双版本）

内置 7 篇常用条款 + Contact Us 联系页面，每篇条款有 8 个不同版本，生成时随机挑选：

- About Us（关于我们）
- Shipping Policy（配送政策）
- Refund Policy（退款政策）
- Privacy Policy（隐私政策）
- Terms of Service（服务条款）
- FAQ（常见问题）
- Contact Us（联系我们）

#### 两个版本切换

| 版本 | 说明 | 域名输入框 |
|------|------|-----------|
| **带域名版本** | 条款中包含可点击的域名链接和邮箱地址 | 可填写 |
| **不带域名版本** | 仅使用品牌名，无任何域名/邮箱内容，文案合规化处理 | 自动禁用 |

> 选择「不带域名」版本时，域名输入框会自动变灰禁用，防止误输入。

---

### 3. CSV 大文件自动分割

当产品 CSV 文件大小超过 **15MB** 时，自动按产品 ID 分组切割成多个小文件：

- 确保同一个产品 ID 的所有行在同一个文件中
- 每个分割文件控制在 15MB 以内
- 文件名格式：`{品牌}_part1.csv`、`{品牌}_part2.csv`...
- 分割文件与 Excel 输出在同一目录

---

### 4. Shopify 主题模板定制

自动生成 `{品牌}_theme.zip` 主题包，对 Shopify 模板进行以下定制修改：

#### 4.1 自定义产品标签（Custom Tabs）

根据 **Metafield Definitions** 表的数据，自动在产品详情页添加自定义标签：

- 标签标题：使用 Name 字段（全部大写）
- 关联字段：使用 Key 字段对应 Metafield
- Block ID：自动生成 `custom_xxxxxx` 格式的 6 位随机 ID
- 插入位置：Description 标签之后、Share 标签之前
- 其他设置：使用模板默认值（移动端展开、显示更多按钮等）

> 如果 Metafield Definitions 为空，自动移除所有 type=custom 的标签。

#### 4.2 销量数据随机化

| 位置 | 字段 | 数量 | 范围 |
|------|------|------|------|
| `product.json` (meta block) | `sold_in_number` | 10 个 | 200 ~ 500 |
| `settings_data.json` | `quick_view_sold_in_number` | 10 个 | 200 ~ 500 |
| `settings_data.json` | `quick_view_customer_viewing_number` | 20 个 | 200 ~ 500 |

> 格式示例：`"231,352,463,574,68,207,282,395"`

#### 4.3 Footer 页脚定制

- **链接列表菜单**：自动为 Footer 中 3 个 `link_list` 类型的区块设置菜单：
  - 第 1 个 → `main-menu`
  - 第 2 个 → `policy`
  - 第 3 个 → `about`
- **版权信息**：自动将版权文字中的域名替换为输入的域名（智能匹配，不依赖固定文本）

---

### 5. 菜单智能生成

#### 商品分类菜单

从 Smart Collections 自动构建多级菜单结构：

- 智能识别产品型号后缀（Pro / Max / Mini / Air 等），避免错误嵌套
- 自动推断中间层级分组（满足单词数 > 2、子项 ≥ 2 等约束）
- 正确处理父子关系，Handle 复用避免 MENU009 错误

#### 页面菜单

自动从 Pages 生成 Policy 和 About 两个分组菜单。

---

### 6. SEO 内容自动生成

完全基于 CSV 产品数据自动生成 SEO 内容，无需手动输入：

- **SEO 标题**：从产品标题提取热门关键词，结合品牌和域名生成
- **SEO 描述**：基于产品关键词和分类模板生成吸引人的描述
- **热门关键词统计**：从产品标题中提取 Top 15 高频关键词用于内容生成

---

## 使用方式

### 输入项（仅 3 个）

1. **品牌名** - 用于条款页面和标题生成
2. **域名** - 用于带域名版本的条款和版权替换（不带域名版本时自动禁用）
3. **产品 CSV 文件** - Shopify 产品导出文件

### 操作步骤

1. 选择条款版本（带域名 / 不带域名）
2. 输入品牌名和域名（不带域名版本可跳过域名）
3. 选择产品 CSV 文件
4. 点击「生成」按钮
5. 生成完成后，输出目录包含：
   - `{品牌}_shopify_altera.xlsx` - Altera 导入 Excel 文件
   - `{品牌}_theme.zip` - 定制后的 Shopify 主题包
   - `{品牌}_partN.csv` - 大文件分割后的 CSV（超过 15MB 时生成）
   - `altera/` - Excel 超限时的独立 CSV 文件（超过 99 行时生成）

---

## 技术特点

### 模板通用性

所有模板修改均通过 **type 类型定位**，不依赖固定的 section/block ID：

- 产品主 section：通过查找 `type=description` 的 block 动态定位
- Custom Tabs：通过 `type=custom` 定位并移除原有标签
- 销量数据：通过 `type=meta` 定位所有相关 block
- Footer 菜单：通过 `type=link_list` 定位并按顺序设置

> 更换不同的 Shopify 2.0 主题模板也能正常工作。

### 资源分离

- EXE 可执行文件 + `data/` 数据文件夹分离式分发
- 模板文件独立存放，方便后续更新和修改
- 打包体积小（约 12MB），启动快

---

## 文件结构

```
release/
├── 豆脚AlteraExcel工具.exe      # 主程序
└── data/
    ├── templates.json            # SEO 内容生成模板
    ├── favicon.png               # 程序图标
    ├── icon.ico                  # 备用图标
    ├── pages_with_domain/        # 带域名版本的条款模板（57个JSON）
    │   ├── about-us_v1.json ~ v8.json
    │   ├── shipping-policy_v1.json ~ v8.json
    │   ├── ...
    │   └── contact-us.json
    ├── pages_without_domain/     # 不带域名版本的条款模板（57个JSON）
    │   ├── about-us_v1.json ~ v8.json
    │   ├── ...
    │   └── contact-us.json
    └── shopify模板/              # Shopify 主题模板源文件
        ├── templates/
        │   └── product.json
        ├── config/
        │   └── settings_data.json
        └── sections/
            └── footer-group.json
```
